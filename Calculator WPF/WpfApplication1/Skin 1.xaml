<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WpfApplication1">

    <Style TargetType="{x:Type Grid}">
        <Setter Property="Background" Value="Crimson"/>
    </Style>

    <LinearGradientBrush x:Key="BrushForButton" StartPoint="0,0"
                         EndPoint="0,1">
        <GradientStop Color="White" Offset="0"/>
        <GradientStop Color="LightBlue" Offset="0.9"/>
    </LinearGradientBrush>

    <Style TargetType="{x:Type Button}" x:Key="ButtonStyle">
        <Setter Property="Background" Value="{DynamicResource BrushForButton}"/>
    </Style>


    <LinearGradientBrush x:Key="BrushForTextBox" StartPoint="0,0"
                         EndPoint="0,1">
        <GradientStop Color="White" Offset="0"/>
        <GradientStop Color="LightGray" Offset="0.9"/>
    </LinearGradientBrush>

    <Style TargetType="{x:Type TextBox}" x:Key="TextBoxStyle">
        <Setter Property="Background" Value="{DynamicResource BrushForTextBox}"/>
    </Style>

    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="Green"/>
    </Style>
</ResourceDictionary>